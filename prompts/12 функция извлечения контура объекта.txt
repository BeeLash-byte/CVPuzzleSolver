реализуй извлечение контура объекта
на вход дана маска, пиксели в которых нули - нужно игнорировать
пиксели в которых 255 - пиксели интереса, они принадлежат объекту

buildContourMask - сначала определи какие пиксели лежат напериметре изображения
(например оставив только те пиксели у которых по стороне и диагонали из 8 соседей - два или один отмечены нулем, а остальные - 255)

extractContour - затем по маске построенного контура - построй цикл (он всегда один) из точек point2i обходящих этот контур
(важно чтобы вершины шли подряд, и они должны обходить контур по часовой стрелке)

напиши так же extract_contour_tests.cpp

#pragma once

#include <libimages/image.h>
#include <libbase/point2.h>

#include <vector>

image8u buildContourMask(const image8u &objectContourMask) {

}

std::vector<point2i> extractContour(const image8u &objectContourMask) {

}
